import styled from "styled-components";

export const Wrapper = styled.div`
  .expenses-list__fallback {
    text-align: center;
    color: white;
  }

  .expenses-list {
    list-style: none;
    padding: 0;
  }
`;
